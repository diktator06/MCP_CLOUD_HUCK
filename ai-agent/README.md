# AI Agent: GitHub Repository Analysis Agent

**LangChain агент для анализа репозиториев GitHub с использованием Evolution Foundation Models**

## 📋 Описание

AI Agent - это интеллектуальный агент, который использует LangChain и Evolution Foundation Models от Cloud.ru для анализа репозиториев GitHub. Агент интерпретирует запросы на естественном языке и автоматически выбирает подходящие MCP инструменты для получения информации.

## 🎯 Возможности

- **Интерпретация естественного языка** - понимание запросов на русском и английском
- **Автоматический выбор инструментов** - агент сам определяет, какой инструмент использовать
- **ModelManager** - динамическое переключение между 11 моделями Evolution Foundation Models
- **Интеграция с MCP серверами** - работа с инструментами через HTTP
- **Формирование человекочитаемых ответов** - структурированные и понятные результаты
- **Обработка ошибок** - корректная обработка несуществующих репозиториев и других ошибок

## 🔧 Компоненты

### ModelManager

Менеджер моделей для динамического переключения между Evolution Foundation Models.

**Доступные модели:**
- `Qwen/Qwen3-Next-80B-A3B-Instruct` (по умолчанию)
- `Qwen/Qwen3-Coder-480B-A35B-Instruct` (для работы с кодом)
- `Qwen/Qwen3-235B-A22B-Instruct` (крупная модель)
- `GigaChat/GigaChat-2-Max`
- `t-tech/T-pro-it-2.0`
- И другие (всего 11 моделей)

**Пример использования:**
```python
from agent import create_agent, ModelManager

# Создание агента с моделью по умолчанию
agent = create_agent(tools)

# Создание агента с конкретной моделью
agent = create_agent(tools, model_name="GigaChat/GigaChat-2-Max")

# Использование категории
agent = create_agent(tools, model_name="coding")
```

### A2A Wrappers

Обертки LangChain для MCP инструментов:

- `GetRepositoryHealthTool` - обертка для `get_repository_health`
- `GetRepositoryIssuesSummaryTool` - обертка для `get_repository_issues_summary`
- `CompareRepositoriesTool` - обертка для `compare_repositories`

## 🚀 Установка и запуск

### Локальный запуск

```bash
# Установка зависимостей
pip install -e .

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env и добавьте:
# - API_KEY (Cloud.ru API ключ)
# - BASE_URL (по умолчанию: https://foundation-models.api.cloud.ru/v1)
# - DEFAULT_MODEL (по умолчанию: Qwen/Qwen3-Next-80B-A3B-Instruct)
# - MCP_SERVER_1_URL (по умолчанию: http://localhost:8000)
# - MCP_SERVER_2_URL (по умолчанию: http://localhost:8001)

# Запуск агента
python src/start_a2a.py
```

### Docker

```bash
# Сборка образа
docker build -t ai-agent .

# Запуск контейнера
docker run --env-file .env ai-agent
```

**Важно:** Перед запуском агента убедитесь, что MCP серверы запущены и доступны.

## ⚙️ Конфигурация

### Переменные окружения

- `API_KEY` (обязательный) - Cloud.ru Evolution Foundation Models API Key
- `BASE_URL` (опциональный) - Base URL для Evolution Foundation Models (по умолчанию: https://foundation-models.api.cloud.ru/v1)
- `DEFAULT_MODEL` (опциональный) - Модель по умолчанию (по умолчанию: Qwen/Qwen3-Next-80B-A3B-Instruct)
- `MCP_SERVER_1_URL` (опциональный) - URL для MCP Server 1 (по умолчанию: http://localhost:8000)
- `MCP_SERVER_2_URL` (опциональный) - URL для MCP Server 2 (по умолчанию: http://localhost:8001)

### Получение Cloud.ru API Key

1. Войдите в платформу Cloud.ru
2. Перейдите в раздел Evolution Foundation Models
3. Создайте API ключ
4. Скопируйте ключ и добавьте в `.env`

## 💡 Примеры использования

### Пример 1: Получение метрик здоровья

```
Пользователь: "What is the health status of langchain-ai/langchain?"
Агент: [Вызывает get_repository_health] → Возвращает детальные метрики
```

### Пример 2: Сравнение репозиториев

```
Пользователь: "Compare langchain-ai/langchain and openai/openai-python"
Агент: [Вызывает compare_repositories] → Возвращает сравнение метрик
```

### Пример 3: Анализ issues

```
Пользователь: "Получи сводку по issues репозитория microsoft/vscode"
Агент: [Вызывает get_repository_issues_summary] → Возвращает сводку по issues
```

## 🧪 Тестирование

```bash
# Установка dev зависимостей
pip install -e ".[dev]"

# Запуск тестов
pytest tests/ -v

# Запуск тестов с покрытием
pytest tests/ --cov=src --cov-report=html
```

## 📊 Архитектура

```
User Query (Natural Language)
        ↓
LangChain Agent (Evolution Foundation Models)
        ↓
A2A Wrappers (HTTP calls)
        ↓
MCP Servers (GitHub API)
        ↓
Structured Response
```

## 📁 Структура проекта

```
ai-agent/
├── src/
│   ├── agent.py           # LangChain агент + ModelManager
│   ├── a2a_wrapper.py     # Обертки для MCP инструментов
│   └── start_a2a.py       # Точка входа с демонстрацией
├── tests/                 # Unit тесты
└── pyproject.toml         # Зависимости
```

## 🔗 Связанные компоненты

- **MCP Server 1** - предоставляет инструменты для анализа здоровья репозиториев
- **MCP Server 2** - предоставляет инструмент для сравнения репозиториев

## 📚 Дополнительная информация

См. главный [README.md](../README.md) для общей информации о проекте.
