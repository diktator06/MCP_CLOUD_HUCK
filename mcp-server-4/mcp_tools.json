[
  {
    "name": "search_code_in_repository",
    "description": "🔍 Ищет код в репозитории GitHub по запросу. Выполняет поиск кода в репозитории: поиск по тексту запроса, фильтрация по языку программирования, поиск в конкретных файлах или директориях, возвращает контекст найденного кода. Используйте этот инструмент для быстрого поиска функций, классов или фрагментов кода в репозитории.",
    "args": [
      {
        "name": "owner",
        "type": "string",
        "description": "Владелец репозитория (username или organization name)",
        "required": true,
        "examples": ["octocat", "microsoft", "facebook"]
      },
      {
        "name": "repo",
        "type": "string",
        "description": "Название репозитория",
        "required": true,
        "examples": ["Hello-World", "vscode", "react"]
      },
      {
        "name": "query",
        "type": "string",
        "description": "Поисковый запрос (текст для поиска в коде)",
        "required": true,
        "examples": ["function calculate", "class User", "import requests"]
      },
      {
        "name": "language",
        "type": "string",
        "description": "Язык программирования для фильтрации (опционально)",
        "required": false,
        "examples": ["Python", "JavaScript", "TypeScript"]
      },
      {
        "name": "path",
        "type": "string",
        "description": "Путь к файлу или директории для ограничения поиска (опционально)",
        "required": false,
        "examples": ["src/", "tests/", "main.py"]
      }
    ]
  },
  {
    "name": "get_file_tree",
    "description": "📁 Получает структуру файлов и директорий репозитория GitHub. Анализирует структуру репозитория: дерево файлов и директорий, размеры файлов, типы файлов, основные директории проекта. Используйте этот инструмент для анализа архитектуры репозитория и понимания структуры проекта.",
    "args": [
      {
        "name": "owner",
        "type": "string",
        "description": "Владелец репозитория (username или organization name)",
        "required": true,
        "examples": ["octocat", "microsoft", "facebook"]
      },
      {
        "name": "repo",
        "type": "string",
        "description": "Название репозитория",
        "required": true,
        "examples": ["Hello-World", "vscode", "react"]
      },
      {
        "name": "path",
        "type": "string",
        "description": "Путь к директории для анализа (пустая строка = корень репозитория, по умолчанию: '')",
        "required": false,
        "examples": ["", "src/", "tests/", "docs/"]
      },
      {
        "name": "max_depth",
        "type": "integer",
        "description": "Максимальная глубина дерева для отображения (по умолчанию: 2, минимум: 1, максимум: 5)",
        "required": false,
        "examples": [2, 3]
      }
    ]
  },
  {
    "name": "analyze_dependencies",
    "description": "📦 Анализирует зависимости репозитория GitHub. Анализирует зависимости проекта: Python (requirements.txt, pyproject.toml, setup.py), JavaScript/TypeScript (package.json), Java (pom.xml, build.gradle), Go (go.mod), Rust (Cargo.toml). Используйте этот инструмент для анализа зависимостей проекта и выявления используемых библиотек.",
    "args": [
      {
        "name": "owner",
        "type": "string",
        "description": "Владелец репозитория (username или organization name)",
        "required": true,
        "examples": ["octocat", "microsoft", "facebook"]
      },
      {
        "name": "repo",
        "type": "string",
        "description": "Название репозитория",
        "required": true,
        "examples": ["Hello-World", "vscode", "react"]
      }
    ]
  }
]

