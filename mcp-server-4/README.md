# MCP Server 4: GitHub Code Search & Analysis

**MCP сервер для поиска и анализа кода в репозиториях GitHub**

## 📋 Описание

MCP Server 4 предоставляет инструменты для поиска кода и анализа структуры репозиториев GitHub. Сервер интегрируется с GitHub REST API v3 и GitHub Code Search API для предоставления детальной информации о коде и зависимостях проектов.

## 🔧 Инструменты

### 1. `search_code_in_repository`

Ищет код в репозитории по текстовому запросу.

**Параметры:**
- `owner` (str, обязательный) - Владелец репозитория
- `repo` (str, обязательный) - Название репозитория
- `query` (str, обязательный) - Поисковый запрос
- `language` (str, опциональный) - Язык программирования для фильтрации
- `path` (str, опциональный) - Путь к файлу или директории

**Возвращает:**
- Список найденных файлов
- Ссылки на найденный код
- Контекст поиска

### 2. `get_file_tree`

Получает структуру файлов и директорий репозитория.

**Параметры:**
- `owner` (str, обязательный) - Владелец репозитория
- `repo` (str, обязательный) - Название репозитория
- `path` (str, опциональный) - Путь к директории (по умолчанию: корень)
- `max_depth` (int, опциональный) - Максимальная глубина дерева (по умолчанию: 2)

**Возвращает:**
- Дерево файлов и директорий
- Размеры файлов
- Типы файлов

### 3. `analyze_dependencies`

Анализирует зависимости проекта из файлов конфигурации.

**Параметры:**
- `owner` (str, обязательный) - Владелец репозитория
- `repo` (str, обязательный) - Название репозитория

**Возвращает:**
- Найденные файлы зависимостей
- Список зависимостей по файлам
- Статистика по зависимостям

**Поддерживаемые форматы:**
- Python: requirements.txt, pyproject.toml, setup.py
- JavaScript/TypeScript: package.json
- Java: pom.xml, build.gradle
- Go: go.mod
- Rust: Cargo.toml

## 🚀 Установка и запуск

### Локальный запуск

```bash
# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env и добавьте GITHUB_TOKEN

# Запуск сервера
python server.py
```

### Docker

```bash
docker build -t mcp-server-4 .
docker run -p 8003:8003 --env-file .env mcp-server-4
```

## 📊 Prometheus метрики

Метрики доступны на порту 9003:
- `http://localhost:9003/metrics`

## 🔗 Интеграция

Сервер использует порт **8003** по умолчанию и доступен по адресу:
- `http://localhost:8003/mcp`

