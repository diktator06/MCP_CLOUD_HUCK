# MCP Server 2: GitHub Repository Comparison Utility

**MCP сервер для параллельного сравнения метрик нескольких репозиториев GitHub**

## 📋 Описание

MCP Server 2 предоставляет инструмент для параллельного сравнения метрик нескольких репозиториев GitHub. Сервер использует `asyncio.gather()` для одновременного выполнения запросов, что значительно ускоряет процесс сравнения.

## 🔧 Инструменты

### `compare_repositories`

Сравнивает метрики нескольких репозиториев GitHub параллельно.

**Параметры:**
- `repositories` (List[Dict[str, str]], обязательный) - Список репозиториев для сравнения
  - Каждый элемент должен содержать `owner` и `repo`
  - Минимум: 2 репозитория
  - Максимум: 5 репозиториев
- `metrics` (List[str], опциональный) - Список метрик для сравнения
  - Если не указан, сравниваются все доступные метрики
  - Доступные метрики: `open_issues`, `open_prs`, `stars`, `forks`, `watchers`, `last_commit_age`

**Возвращает:**
- Сравнение метрик по всем репозиториям
- Определение наиболее активного репозитория
- Определение наиболее популярного репозитория
- Определение репозитория с наибольшим количеством форков

**Пример использования:**
```python
result = await compare_repositories(
    repositories=[
        {"owner": "microsoft", "repo": "vscode"},
        {"owner": "facebook", "repo": "react"}
    ],
    metrics=["stars", "forks", "last_commit_age"],
    ctx=context
)
```

## 🚀 Установка и запуск

### Локальный запуск

```bash
# Установка зависимостей
pip install -e .

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env и добавьте GITHUB_TOKEN

# Запуск сервера
python server.py
```

### Docker

```bash
# Сборка образа
docker build -t mcp-server-2 .

# Запуск контейнера
docker run -p 8001:8001 --env-file .env mcp-server-2
```

## ⚙️ Конфигурация

### Переменные окружения

- `GITHUB_TOKEN` (обязательный) - GitHub Personal Access Token
- `PORT` (опциональный) - Порт сервера (по умолчанию: 8001)
- `HOST` (опциональный) - Хост сервера (по умолчанию: 0.0.0.0)

## 🧪 Тестирование

```bash
# Установка dev зависимостей
pip install -e ".[dev]"

# Запуск тестов
pytest tests/ -v

# Запуск тестов с покрытием
pytest tests/ --cov=tools --cov-report=html
```

## 📊 Особенности реализации

- **Параллельные запросы:** Использование `asyncio.gather()` для одновременного получения данных
- **Retry механизм:** Автоматические повторы при временных ошибках
- **Rate Limiting:** Защита от превышения лимитов GitHub API
- **OpenTelemetry трейсинг:** Отслеживание операций
- **Обработка ошибок:** Детальные сообщения об ошибках через McpError

## 🔄 Параллельное выполнение

Сервер использует параллельное выполнение запросов для ускорения процесса:

```python
# Все репозитории обрабатываются одновременно
tasks = [
    fetch_repository_data(client, repo["owner"], repo["repo"])
    for repo in repositories
]
results = await asyncio.gather(*tasks, return_exceptions=True)
```

Это позволяет получить данные о 5 репозиториях за время одного запроса вместо 5 последовательных запросов.

## 📁 Структура проекта

```
mcp-server-2/
├── server.py              # Точка входа сервера
├── mcp_instance.py        # Единый экземпляр FastMCP
├── tools/
│   ├── comparison_tool.py # Инструмент: compare_repositories
│   └── utils.py           # Утилиты (retry, rate limiting)
├── tests/                 # Unit тесты
└── pyproject.toml         # Зависимости
```

## 🔗 Связанные файлы

- `mcp-server-catalog.yaml` - Каталог MCP сервера
- `mcp_tools.json` - JSON описание инструментов
- `env_options.json` - Описание переменных окружения

## 📚 Дополнительная информация

См. главный [README.md](../README.md) для общей информации о проекте.
