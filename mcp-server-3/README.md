# MCP Server 3: GitHub Activity Analytics

**MCP сервер для анализа активности репозиториев GitHub**

## 📋 Описание

MCP Server 3 предоставляет инструменты для анализа активности разработки в репозиториях GitHub. Сервер интегрируется с GitHub REST API v3 и предоставляет детальную статистику по коммитам, разработчикам и веткам.

## 🔧 Инструменты

### 1. `get_commit_statistics`

Получает статистику коммитов репозитория за указанный период.

**Параметры:**
- `owner` (str, обязательный) - Владелец репозитория
- `repo` (str, обязательный) - Название репозитория
- `since` (str, опциональный) - Начало периода (по умолчанию: "30 days ago")
- `until` (str, опциональный) - Конец периода (по умолчанию: "now")

**Возвращает:**
- Общее количество коммитов
- Статистика по периодам
- Активность по дням недели
- Топ авторов коммитов

### 2. `get_developer_activity`

Анализирует активность разработчиков в репозитории.

**Параметры:**
- `owner` (str, обязательный) - Владелец репозитория
- `repo` (str, обязательный) - Название репозитория
- `top_n` (int, опциональный) - Количество топ разработчиков (по умолчанию: 10)

**Возвращает:**
- Топ контрибьюторы по коммитам
- Статистика активности
- Распределение активности

### 3. `get_branch_analysis`

Анализирует структуру веток репозитория.

**Параметры:**
- `owner` (str, обязательный) - Владелец репозитория
- `repo` (str, обязательный) - Название репозитория
- `days_threshold` (int, опциональный) - Порог дней для активной ветки (по умолчанию: 90)

**Возвращает:**
- Список всех веток
- Активные и неактивные ветки
- Защищенные ветки
- Статистика по веткам

## 🚀 Установка и запуск

### Локальный запуск

```bash
# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env и добавьте GITHUB_TOKEN

# Запуск сервера
python server.py
```

### Docker

```bash
docker build -t mcp-server-3 .
docker run -p 8002:8002 --env-file .env mcp-server-3
```

## 📊 Prometheus метрики

Метрики доступны на порту 9002:
- `http://localhost:9002/metrics`

## 🔗 Интеграция

Сервер использует порт **8002** по умолчанию и доступен по адресу:
- `http://localhost:8002/mcp`

