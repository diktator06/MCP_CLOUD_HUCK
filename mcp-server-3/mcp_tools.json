[
  {
    "name": "get_commit_statistics",
    "description": "📊 Получает статистику коммитов репозитория GitHub. Анализирует активность коммитов: общее количество коммитов, статистика по периодам (дни, недели, месяцы), активность по дням недели, топ авторов коммитов, график активности за период. Используйте этот инструмент для анализа активности разработки в репозитории.",
    "args": [
      {
        "name": "owner",
        "type": "string",
        "description": "Владелец репозитория (username или organization name)",
        "required": true,
        "examples": ["octocat", "microsoft", "facebook"]
      },
      {
        "name": "repo",
        "type": "string",
        "description": "Название репозитория",
        "required": true,
        "examples": ["Hello-World", "vscode", "react"]
      },
      {
        "name": "since",
        "type": "string",
        "description": "Начало периода для анализа (формат: 'YYYY-MM-DD' или 'N days ago', по умолчанию: '30 days ago')",
        "required": false,
        "examples": ["30 days ago", "2024-01-01"]
      },
      {
        "name": "until",
        "type": "string",
        "description": "Конец периода для анализа (формат: 'YYYY-MM-DD' или 'now', по умолчанию: 'now')",
        "required": false,
        "examples": ["now", "2024-12-31"]
      }
    ]
  },
  {
    "name": "get_developer_activity",
    "description": "👥 Получает статистику активности разработчиков репозитория GitHub. Анализирует активность разработчиков: топ контрибьюторы по количеству коммитов, активность по периодам (недели, месяцы), статистика по авторам pull requests, распределение активности между разработчиками. Используйте этот инструмент для анализа активности команды разработки.",
    "args": [
      {
        "name": "owner",
        "type": "string",
        "description": "Владелец репозитория (username или organization name)",
        "required": true,
        "examples": ["octocat", "microsoft", "facebook"]
      },
      {
        "name": "repo",
        "type": "string",
        "description": "Название репозитория",
        "required": true,
        "examples": ["Hello-World", "vscode", "react"]
      },
      {
        "name": "top_n",
        "type": "integer",
        "description": "Количество топ разработчиков для отображения (по умолчанию: 10, минимум: 1, максимум: 50)",
        "required": false,
        "examples": [10, 20]
      }
    ]
  },
  {
    "name": "get_branch_analysis",
    "description": "🌿 Получает анализ веток репозитория GitHub. Анализирует структуру веток репозитория: список всех веток, активные ветки (с недавними коммитами), защищенные ветки, мертвые ветки (без активности), статистика по веткам. Используйте этот инструмент для анализа структуры репозитория и выявления неиспользуемых веток.",
    "args": [
      {
        "name": "owner",
        "type": "string",
        "description": "Владелец репозитория (username или organization name)",
        "required": true,
        "examples": ["octocat", "microsoft", "facebook"]
      },
      {
        "name": "repo",
        "type": "string",
        "description": "Название репозитория",
        "required": true,
        "examples": ["Hello-World", "vscode", "react"]
      },
      {
        "name": "days_threshold",
        "type": "integer",
        "description": "Порог дней для определения активной ветки (по умолчанию: 90, минимум: 1, максимум: 365)",
        "required": false,
        "examples": [90, 30, 180]
      }
    ]
  }
]

